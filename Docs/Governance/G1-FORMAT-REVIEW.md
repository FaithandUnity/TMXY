# P1/G1 format review

## Decision

P1-28 is complete and the G1 format gate is approved. The machine review is
`Data/BuildBaseline/p1-28-g1-format-review.json`; it is regenerated by
`Tests/Review/New-G1FormatReview.ps1` and verified by
`Tests/Contract/Test-G1FormatReview.ps1`.

The project lead's 2026-08-28 authorization permits P1-28, the G1 format-gate
decision, continued G1-dependent development, GitHub checkpoint pushes, and
verified local backups. Its machine record is
`Data/Governance/p1-g1-stage-authorization.json`. It supersedes only the G1
prohibition in WVR-0001; all other rights, Secret, legacy read-only, G0, P0,
hosted-CI, and release-authority boundaries remain in force.

## Review result

All 27 prerequisites pass their committed evidence contracts. All eight G1
criteria pass:

1. Package 1.0 remains covered by golden tests.
2. Package 2.0 and 3.0 core samples are stable and deterministic.
3. Twenty current TBL representatives decode and validate with a keychain-only
   runtime key reference; no raw key or plaintext is emitted.
4. QTX, SM, SKEM, ANIM, and TER have structured intermediate output.
5. Each of those five resource types has a real UE 5.8.2 golden import.
6. Coordinates, units, UVs, skeletons, animation, and terrain orientation are
   covered by bounded tests and UE Automation evidence.
7. The golden matrix contains nine corrupt-input cases and fails closed.
8. Reference documentation preserves evidence levels and unknown fields.

The review keeps ten inherited or newly exposed unknown items. Every item has a
named workstream owner, an isolation rule, and a P2 follow-up task. Unknown data
continues to be preserved as bytes, opaque source spans, or explicit failures;
it is never silently guessed.

## Authority boundary

G1 proves format-readiness and opens the G1-dependent P2 tasks P2-01, P2-04,
and P2-12. It does not mark P0-12/P0-16 complete, approve G0, create release
authority, authorize writes to legacy directories, expose the TBL runtime key,
or grant redistribution rights for legacy assets. Those states remain separate
and fail closed.
