# Golden sample references

This directory stores portable sample-selection metadata only. It never stores
copied legacy packages, client assets, table payloads, decryption material, or
bulk exports.

- `p0-golden-selection.json` is the reviewed selection source.
- `p0-golden-samples.json` is generated from the frozen client Manifest and
  verified against the read-only `3.0.0.413` client.

The source files remain under `E:\QQXYCodeDev\天命西游` and are addressed by
relative path plus SHA-256.

